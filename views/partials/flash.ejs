<% if(success && success.length) {%>
    <div class="successFlash">
        <%= success %>
            <button class="hide">
                <span>&times;</span>
            </button>
    </div>
    <% } %>

        <% if(error && error.length) {%>
            <div class="errorFlash" role="alert">
                <b>Error:</b>
                <%= error %>
                    <button class="hide">
                        <span>&times;</span>
                    </button>
            </div>
            <% } %>

                <% if(alert && alert.length) {%>
                    <div class="alertFlash" role="alert">
                        <b>Alert:</b>
                        <%= alert %>
                            <button class="hide">
                                <span>&times;</span>
                            </button>
                    </div>
                    <% } %>